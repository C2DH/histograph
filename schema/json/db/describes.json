{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://c2dh.uni.lu/histograph/db/describes.json",
  "title": "Describes",
  "description": "Describes relationship between Version and Resource",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "creation_date": {
      "description": "Resource creation ISO timestamp",
      "type": "string",
      "format": "date-time"
    },
    "creation_time": {
      "description": "Resource creation UNIX time in milliseconds",
      "type": "integer"
    },
    "last_modification_date": {
      "description": "Resource modification ISO timestamp",
      "type": "string",
      "format": "date-time"
    },
    "last_modification_time": {
      "description": "Resource modification UNIX time in milliseconds",
      "type": "integer"
    }
  },
  "required": [
  ]
}